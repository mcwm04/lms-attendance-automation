"""
=========================================================
Layout Helpers (currently unused)
LMS Attendance Automation System
=========================================================

This module is not imported anywhere in the current
codebase — app.py builds the screen-host / authentication /
dashboard structure directly.

Left as an intentional placeholder: if you want to move that
scaffolding out of app.py in the future (e.g. a
`build_app_shell()` helper that wraps the authentication and
dashboard screens inside the `screen-host` column), it belongs
here. Until then this file can be safely deleted without
affecting the app.
"""
